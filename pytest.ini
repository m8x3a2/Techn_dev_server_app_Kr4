# pytest.ini — конфигурация pytest
[pytest]
# Указываем pytest-asyncio использовать автоматический режим
# (не нужно добавлять @pytest.mark.asyncio вручную к каждому тесту)
asyncio_mode = auto

# Папки с тестами
testpaths = tests

# Показывать подробный вывод
addopts = -v
